# Plugins

Cowork plugins, MCP wrappers, and agent configurations. These are the infrastructure layer — tools that extend what an AI agent can do in a PM workflow.

---

## Index

*Coming soon. First plugin will be a PM workflow pack: bundled skills + MCP connections for the tools a platform PM uses daily.*

---

## What counts as a plugin here

A plugin graduates from the skills folder when it has its own non-trivial setup, wraps an external MCP or API, or is something another PM could install and run independently in their own environment.
